import React from 'react'

export default UserLocation = (props) => {
    // Geocoder.geocodePosition({lat:pos.latitude,lng:pos.longitude}).then(res => {
    //     if(pos.latitude != 0 && pos.longitude != 0){
    //         setNewLocation({
    //             complete:res[0].formattedAddress,
    //             street:`${res[0].streetNumber} ${res[0].streetName}`,
    //             city:res[0].locality,
    //             state:res[0].adminArea,
    //             zip:res[0].postalCode, 
    //         })
    //         //updateLocation(pos,res[0].formattedAddress,`${res[0].streetNumber} ${res[0].streetName}`,res[0].locality,res[0].adminArea,res[0].postalCode)
    //     }
    // }).catch(err => console.log(err))
    return null
}